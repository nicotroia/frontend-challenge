import React from 'react';

// import styles from './styles.scss';

export type Props = {};

const App = (props: Props): JSX.Element => {
  const { } = props;

  return (
    <div>      
      <h1>App</h1>
    </div>
  );
}

export default App;